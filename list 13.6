//list 13.6

#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
	//파일열기
	ifstream input("state.txt");

	if (input.fail())
	{
		cout << "File does not exist" << endl;
		cout << "Exit program" << endl;
		return 0;
	}

	//데이터읽기
	string state;

	while (!input.eof())
	{
		getline(input, state, '#');
		cout << state << endl;
	}

	input.close();
	cout << "Done" << endl;

	return 0;
}
